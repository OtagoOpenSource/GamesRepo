#ignore unwanted files from the git repository

#private user-specific settings should not be shared.
/nbproject/private

#generated by Macs
.DS_Store

# backup files created by emacs
*~

#don't want java classes in the repo
/build/
/dist/

#we don't want platform specific files creeping into the repo.
#jmonkey spits them out when it builds on the system.
*.dylib
*.dll
*.jnilib
*.so
